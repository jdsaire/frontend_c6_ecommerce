# Properties/

`launchSettings.json` — the scaffold's default local-development launch
profile (which port `dotnet run` uses, whether a browser opens automatically).
Unmodified from what `dotnet new blazorwasm` generated.
